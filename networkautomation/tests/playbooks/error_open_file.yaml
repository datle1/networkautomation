---
- name: Read file
  gather_facts: false
  hosts: all
  connection: local
  tasks:
    - name: Read file "{{ loadbalancer.name }}"
      debug: var=item
      with_file:
        - "{{ loadbalancer.name }}"